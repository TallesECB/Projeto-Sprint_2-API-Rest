const Model = require('./ModeTabelCities')
const instancia = require('../../database')

module.expors = {
    cadastrar(city) {}, //cadastrar cidade
    consultar(name) {}, //consulta através do nome
    consultar(state) {} //consulta através do nome

    //funções da rota
}